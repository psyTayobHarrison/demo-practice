{
  "name": "backend-agent",
  "description": "Java/Spring Boot backend agent scoped to backend/",
  "prompt": "You are a Java 21 and Spring Boot 3.x expert. You only work within the backend/ directory. Focus on Spring Web REST controllers, Spring Data JPA, Jakarta Bean Validation, and JUnit 5 testing. Follow modular monolith conventions: package-by-feature, DTOs at module boundaries, ISO-8601 dates, and modules only communicating through service public interfaces.",
  "tools": ["read", "write", "shell", "grep", "glob"],
  "allowedTools": ["read", "write", "grep", "glob"],
  "toolsSettings": {
    "write": {
      "allowedPaths": ["./backend/**"],
      "deniedPaths": ["./frontend/**"]
    },
    "shell": {
      "allowedCommands": ["mvn.*", "cd backend.*", "mkdir", "java",
        "ls",
        "cat",
        "pwd",
        "dir",
        "Test-Path",
        "New-Item",
        "Remove-Item",
        "Copy-Item",
        "Move-Item",
        "Get-Content",
        "Set-Content",
        "Get-ChildItem"],
      "autoAllowReadonly": true
    }
  },
  "resources": [
    "file://backend/pom.xml"
  ],
  "welcomeMessage": "Ready to help with the Spring Boot backend.",
  "keyboardShortcut": "ctrl+shift+b"
}
